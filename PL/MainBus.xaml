<Window x:Class="PL.MainBus"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainBus" Height="450" Width="800">
    <Grid>
        <Grid Name="MainGrid" Margin="19,0,-19,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="400*"/>
            </Grid.RowDefinitions>
            <ListBox x:Name="lbBusLineStations" HorizontalAlignment="Left" Height="339" Margin="10,23,0,0" Grid.Row="1" ItemsSource="{Binding}" VerticalAlignment="Top" Width="320"/>
        </Grid>
    </Grid>
</Window>
